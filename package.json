{
  "name": "sop-ui",
  "version": "0.2.0",
  "private": true,
  "description": "SOP UI — prototype is the active implementation",
  "scripts": {
    "dev": "cd prototype && npm run dev",
    "build": "cd prototype && npm run build",
    "ai-api-cli": "node scripts/ai-api-cli.mjs"
  }
}
